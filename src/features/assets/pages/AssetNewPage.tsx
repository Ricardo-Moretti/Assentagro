import React from 'react';
import { AssetForm } from '../components/AssetForm';

export const AssetNewPage: React.FC = () => {
  return (
    <div className="animate-fade-in">
      <AssetForm />
    </div>
  );
};
